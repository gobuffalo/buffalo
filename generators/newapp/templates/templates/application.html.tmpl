<html>
  <head>
    <meta charset="utf-8">
    <title>Buffalo - {{ .titleName }}</title>
    <link rel="stylesheet" href="<%= assetPath("application.css") %>" type="text/css" media="all" />
    <meta name="csrf-param" content="authenticity_token" />
    <meta name="csrf-token" content="<%= authenticity_token %>" />
  </head>
  <body>

    <div class="container">
      <%= partial("flash.html") %>
      <%= yield %>
    </div>
    
    <link rel="stylesheet" href="<%= assetPath("application.css") %>" type="text/css" media="all" />
  </body>
</html>
