<h1>New {{.model}}</h1>

<%= form_for({{.varSingular}}, {action: {{.routeFirstDown}}Path(), method: "POST"}) { %>
  <%= partial("{{.filesPath}}/form.html") %>
  <a href="<%= {{.routeFirstDown}}Path() %>" class="btn btn-warning" data-confirm="Are you sure?">Cancel</a>
<% } %>
