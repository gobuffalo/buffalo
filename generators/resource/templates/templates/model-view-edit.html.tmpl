<h1>Edit {{.model}}</h1>

<%= form_for({{.varSingular}}, {action: {{.model}}Path({{.under}}_id: {{.varSingular}}.ID), method: "PUT", errors: errors}) { %>
  <%= partial("{{.modelPluralUnder}}/form.html") %>
  <a href="<%= {{.model}}Path({{.under}}_id: {{.varSingular}}.ID) %>" class="btn btn-warning" data-confirm="Are you sure?">Cancel</a>
<% } %>
