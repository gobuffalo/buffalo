version: "{build}"
clone_depth: 5
clone_folder: c:\gopath\src\github.com\gobuffalo\buffalo

environment:
  GOPATH: c:\gopath

stack: go 1.11

build:
  verbosity: normal

deploy: false

test_script:
  - set GO111MODULE=on
  - go test -race ./...
